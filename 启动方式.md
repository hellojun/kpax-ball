启动方式
后端：


cd backend
cp .env.example .env  # 填入 DEEPSEEK_API_KEY
.venv/bin/uvicorn app.main:app --reload --port 8003
插件：


cd extension
npm run build:dev    # → dist-dev/，自测连生产后端（dev manifest，固定 ext id）
npm run build:prod   # → dist-prod/，打包发同事（prod manifest）
# 在 chrome://extensions 加载 dist-dev/ 或 dist-prod/ 目录作为未打包插件
Docker 部署：


docker-compose up